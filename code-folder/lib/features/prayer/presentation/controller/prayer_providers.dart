// Riverpod providers/controllers for prayer.
// Define your Notifiers, Controllers, and Providers here.
// Example:
// final prayerControllerProvider = StateNotifierProvider<PrayerController, PrayerState>((ref) {
//   final repo = ref.watch(prayerRepositoryProvider);
//   return PrayerController(repo);
// });
