// Domain repository interface for prayer.
abstract class PrayerRepository {
  // Define the methods your presentation/domain will use.
  // Example:
  // Future<Result<User>> login({required String username, required String password});
}
