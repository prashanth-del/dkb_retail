name: async_ui
description: Global lock overlay + AsyncView/RxView for Riverpod apps
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=3.4.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
